# Data Directory

This directory is structurally intact to replicate the exact architecture of the project repository. However, specific files mapping to raw sequences, matrices, and compiled objects (`.rds`, `.h5ad`, `.csv`) exceeding 100 MB have been explicitly ignored to comply with GitHub file size limits.

Users cloning this repository should populate this directory using the primary download scripts located in `scripts/`.
