# Models Directory

This directory is structurally intact to replicate the exact architecture of the project repository. However, large pre-trained weights and compiled network models (e.g., `.pt`, `.h5`) exceeding 100 MB have been explicitly ignored to comply with GitHub file size limits.

All machine learning inferences draw from weights located or synthesized identically here upon execution.
